#include <iostream>
#include <vector>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    long long n;
    short int k;
    cin >> n >> k;
    vector <char> ka;
    while (n >= k) {
        int tmp = n % k;
        char dig;
        if ( 0 <= tmp && tmp <= 9) {
            
        }
    }
}